version: '3.8'

services:
  api:
    build:
      context: ./api
      dockerfile: dockerfile/Dockerfile
    ports:
      - "5000:5000"
    environment:
      - GOOGLE_API_KEY=AIZaSyB-q2NAqq5r2INU0yXat9VV1gEqtVRpidw

  streamlit:
    build:
      context: ./streamlit
      dockerfile: dockerfile/Dockerfile
    ports:
      - "8501:8501"
